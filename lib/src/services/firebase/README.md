# Firebase Setup

1. Add packages such as `firebase_core`, `firebase_auth`, and `cloud_firestore`.
2. Run `flutterfire configure`.
3. Place the generated `firebase_options.dart` in this folder.
4. Update `firebase_bootstrap.dart` to call `Firebase.initializeApp(...)`.
